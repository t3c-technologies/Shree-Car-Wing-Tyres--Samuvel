/** @type {import('next-sitemap').IConfig} */
const config = {
  siteUrl: 'https://shreecaarwindtyres.in', // Replace with your domain
  generateRobotsTxt: true,          // Generate robots.txt
  sitemapSize: 5000,                // Optional: Adjust if your site has a large number of pages
};

module.exports = config;
